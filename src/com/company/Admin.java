package com.company;

public class Admin extends User{
    //DataBase dataBase; Should see the full database
    // Should be able to manipulate database
    public Admin(Account account) {
        super(account);
    }

}
